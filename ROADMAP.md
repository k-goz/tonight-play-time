# 今晚还能玩多久：M5-M11 交付状态

本轮把原路线中的 M5-M10 合并为一个家庭平台大版本。开发原则不变：每项能力同时具备可交互界面、服务端数据边界、数据库迁移、自动化回归和真实浏览器验证。

## M5：家庭启动台与设备模式 — 已完成

- 头像式家庭启动台，显示每个孩子的当晚睡觉规则
- 本设备默认孩子，刷新和重新打开后直接进入对应档案
- 家长控制孩子是否可以自行切换；儿童页不暴露账号、归档和规则管理
- 多孩子本地缓存仍按“账号 + 孩子”隔离

## M6：计划与提醒 V1 — 已完成

- 每个孩子独立的提醒开关、开始时间和暂停过久阈值
- 今日未开始、暂停过久、临睡 15 分钟三类调度
- PWA 系统通知；权限拒绝或不支持时降级为应用内提示
- 服务端记录触达、阻止、不支持和失败状态，家长可查看最近结果

V1 边界：浏览器必须处于运行状态才能本地调度；完全关闭后的云推送需要后续接入 Web Push 供应端和订阅密钥。

## M7：成长激励与周目标 — 已完成

- 连续完成、1-7 天周目标、平均作业用时与平均睡前余量
- 可配置家庭奖励，保留家长确认和兑现状态
- 每周一次中断保护；保护连续记录但不虚增完成天数
- 多孩子独立周报和历史星星图

## M8：同步冲突与离线可靠性 — 已完成

- 每条会话记录带单调递增版本号和更新时间
- 普通计时更新断网时进入本机队列，联网后自动合并重试
- 旧版本写入生成服务端冲突记录，不静默覆盖
- 家长同步中心可以明确选择保留云端或采用本设备
- JSON 导出 / 恢复导入，以及 SQLite 备份 / 恢复演练

安全边界：账号模式离线时不能绕过家长 PIN 完成任务；一次性完成确认仍要求联网验证。

## M9：生产运营与质量体系 — 已完成（移动端实机矩阵继续跟踪）

- SQLite `VACUUM INTO` 一致性备份、保留策略、恢复前安全副本和完整性检查
- 登录和 PIN 关键接口内存限流
- 家长授权、奖励、冲突、家庭偏好和试用行为审计
- `/api/health`、家长运行面板、Schema 版本和冲突 / 备份状态
- `npm run quality`、20 项自动化测试、真实浏览器桌面 / 390px 手机回归

Vercel HTTPS、云数据库、跨部署持久化、恢复工具和回滚路径已验证。Safari / iOS PWA、Android Chrome PWA、通知降级和真实恢复窗口继续纳入封闭测试环境验收，不阻塞当前 Web 版本上线。

## M10：付费验证 — 已完成

- 免费版 / 家庭版体验状态和 14 天无扣款体验
- ¥9/月、¥19/月、¥99/年三个价格点意向记录
- 自由文本付费原因反馈
- 激活、活跃天数、7 日留存、近 7 天家庭使用次数、孩子数和付费意向指标
- 明确不接真实支付、不自动续费；先用真实家庭访谈和意向数据验证付费价值

## 下一阶段建议（M12）

1. 用 10-20 个真实家庭跑 2 周封闭测试。
2. 先补齐 iOS / Android PWA 实机矩阵和自动备份定时任务。
3. 根据提醒触达率、7 日留存与 ¥19/月意向决定是否接 Web Push 和支付。
4. 只有付费意向成立后，再进入正式订阅、运营后台和多租户扩展。

## M11：生产级全栈云端化 — 已完成

- Express API 通过 Vercel Function 与静态 PWA 同源部署
- Neon PostgreSQL 新加坡区连接池与加密环境变量
- SQLite V1-V5 自动升级、PostgreSQL V6 幂等 Schema 和迁移工具
- PostgreSQL 完整家庭生命周期、跨账号隔离和持久化端到端测试
- PostgreSQL 一致性备份、空库恢复、Schema/外键索引检查
- Vercel 安全响应头、API 禁止缓存和显式生产回滚脚本
- Neon HTTPS 瞬时连接故障安全重试与冷启动自愈
- 两个独立 Vercel 部署间登录、任务状态和云端数据持久化验证
